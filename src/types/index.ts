export type AccountType = "personal" | "professional" | "hospital"
export type FeatureContent = {heading: string, info: string}
export type AddFeatureContent = {heading: string, info: string, image: string}
